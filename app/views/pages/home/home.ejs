<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="shortcut icon" href="img/triangle.png" />
    <link rel="stylesheet" href="css/home/home.style.css">
    <title>CPD - Controle</title>
</head>
<body class="login-container">
    <div class="container-fluid d-flex p-0 flex-column align-items-center">
        <div class="d-flex justify-content-between w-100 mt-5 layout">
            <aside class="col-0 col-md-6 d-flex flex-column aside">
                <div class="w-100 ml-5">
                    <img src="img/computer.png" class="computer" alt="icon-computer">
                </div>
                <div class="w-100 d-flex justify-content-center">
                    <img src="img/workplace.png" class="workplace" alt="workplace">
                </div>
                <div class="w-100 d-flex justify-content-end"> 
                    <img src="img/triangle.png" class="triangle" alt="triangle">
                </div>
            </aside>
            <main class="col-12 col-md-6 col-sm-12 pt-5 pb-5 w-100">
                <form action="/dashboard" method="POST" class="d-flex flex-column align-items-center">
                    <img src="img/user.svg" alt="icon" class="user-icon mb-4">
                    <p class="title">Login</p>
                    <input type="text" value="<%= username %>" class="input form-control mb-3 w-75 pt-4 pb-4" name="username" placeholder="Usuário">
                    <div class="d-flex w-100 align-items-center justify-content-center">
                        <input type="password" class="input input-pass form-control mb-4 w-75 pt-4 pb-4 ml-4" value="<%= password %>"
                            name="password" placeholder="Senha">
                        <i class="fas fa-eye-slash eyes mb-3 ml-1"></i>
                        <!-- <i class="fas fa-eye ml-1 mb-3 eyes"></i>   -->
                    </div>
                    <% if(errors.length > 0) { %>
                        <div class="row w-75">
                            <div class="col-md-12 col-12">
                                <div class="alert alert-info">
                                    <strong>Atenção</strong>
                                    <ul>
                                        <% for(let i=0; i<errors.length; i++) { %>
                                            <li><%= errors[i].msg %></li>
                                        <% } %>
                                    </ul>
                                </div>
                            </div>
                        </div>
                    <% } %>
                    <input type="submit" value="Login" class="btn btn-login pt-2 pb-2 w-75 font-weight-bold">
                    <div class="mt-3 d-flex justify-content-end w-75">
                        <a href="">Esqueci minha senha</a>
                    </div>
                </form>
            </main>
        </div>
        <footer class="fixed-bottom d-flex justify-content-center">
            <p>Desenvolvido por - Diego Costa</p>
        </footer>
    </div>
    <script src="js/jquery.3.4.1.js"></script>
    <script src="js/login/main.js" type="module"></script>
</body>
</html>