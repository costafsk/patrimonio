<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="css/home/home.style.css">
    <title>CPD - Controle</title>
</head>
<body class="login-container">
    <div class="container-fluid d-flex flex-column align-items-center">
        <header class="mt-5 w-50 d-flex">
            <div class="w-100">
                <h4 class="mt-3 mb-4 text-white title text-center">CPD Controle</h4>
                <hr class="w-100 ">
            </div>
        </header>
        <main class="col-12 col-md-6 col-sm-12 main-login">
            <form action="/dashboard" method="POST" class="d-flex flex-column align-items-center">
                <input type="text" class="form-control mb-3 w-75 pt-4 pb-4" name="username" placeholder="Usuário">
                <input type="password" class="form-control mb-4 w-75 pt-4 pb-4" name="password" placeholder="Senha">
                <% if(errors.length > 0) { %>
                    <div class="row w-75">
                        <div class="col-md-12 col-12">
                            <div class="alert alert-warning">
                                <strong>Atenção</strong>
                                <ul>
                                    <% for(let i=0; i<errors.length; i++) { %>
                                        <li><%= errors[i].msg %></li>
                                    <% } %>
                                </ul>
                            </div>
                        </div>
                    </div>
                <% } %>
                <input type="submit" value="Login" class="btn btn-login pt-2 pb-2 w-75 font-weight-bold">
                <div class="d-flex justify-content-end w-100">
                    <a href="#" class="mr-5 mt-5" id="noPass">Esqueci minha senha</a>
                </div>
            </form>
        </main>
        <footer class="footer fixed-bottom bg-dark d-flex flex-row justify-content-end">
            <p class="text-white mt-3 mr-5">CPD - Todos Direitos Reservados &copy; 2019</p>
        </footer>
    </div>
    <script src="js/main.js" type="module"></script>
</body>
</html>